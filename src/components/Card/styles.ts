import styled from 'styled-components';

import { colors } from '../../styles';

export const Wrapper = styled.div`
  display: flex;
  background-color: ${colors.white};
  border-radius: 3px;
  box-shadow: 0 0 18px ${colors.blackA3};
`;
